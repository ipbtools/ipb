# Action script: lock/unlock and scroll.
# Format: <seconds><TAB><prompt>. Do exactly what each prompt says, then wait
# for the next one. Do nothing at all during the IDLE steps — they establish
# the background report rate that everything else is measured against.
6	IDLE — do not touch anything (baseline)
6	Click once in the Device Hub window to focus it, then stop
8	Press Command-L to LOCK the screen. Then stop.
8	Press any key / Command-L again to UNLOCK. Then stop.
6	IDLE — do not touch anything
8	Two-finger scroll DOWN on the trackpad, twice, over the phone view
8	Two-finger scroll UP on the trackpad, twice, over the phone view
6	IDLE — do not touch anything
